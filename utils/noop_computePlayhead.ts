export function computePlayhead(duration: number, elapsedTime: number) {}

/* export function computePlayhead(currentTime: number, duration: number) {
  const hasDuration = typeof duration === "number" && isFinite(duration);
  return hasDuration ? currentTime / duration : 0;
} */
